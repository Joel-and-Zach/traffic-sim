from trafficsim.car import Car
from trafficsim.road import Road
import random

#change change
class Simulation:
    """
    Responsibilites:
    Increments time by seconds
    Moves cars, and decides
    """
    def __init__(self, length=500):
        self.time = 0
        self.road = Road()

    def simulation(self):
        while length > time:


    def adjust_speed(self, road):
        for car in self.road.traffic[::-1]:
            if  random.random() > self.road.difficulty:
                if car.current_speed + 2 > 33.33:
                    pass
                car.speedup():
            else:
